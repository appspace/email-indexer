
1. To (re)compile the project, use: `./compile` command. 

2. To run indexing, use `./index {path to folder}`. You can use relative or absolute paths.

3. To execute search, use `./search {word to find}`.